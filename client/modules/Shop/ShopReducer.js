// Import Actions
import {  } from './ShopActions';

// Initial State
const initialState = {};

const ShopReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default ShopReducer;
